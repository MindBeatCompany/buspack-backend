import { UpdateGeneralSettingsDTO } from './update-general-settings.dto';

describe('UpdateGeneralSettings', () => {
  it('should be defined', () => {
    expect(new UpdateGeneralSettingsDTO()).toBeDefined();
  });
});
