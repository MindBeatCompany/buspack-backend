import { CreateGeneralSettingsDTO } from './create-general-settings.dto';

describe('CreateGeneralSettings', () => {
  it('should be defined', () => {
    expect(new CreateGeneralSettingsDTO()).toBeDefined();
  });
});
